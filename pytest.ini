[pytest]
testpaths = tests legacy_streamlit
python_files = test_*.py
python_functions = test_*
# I test che eseguono SQL vero avviano un Postgres locale (tests/conftest_sql.py):
# il marker permette di isolarli con `-m sql` o escluderli con `-m "not sql"`.
markers =
    sql: esegue la logica SQL su un Postgres locale effimero
